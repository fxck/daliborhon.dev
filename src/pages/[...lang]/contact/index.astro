---
import MainLayout from "@layouts/MainLayout.astro";

export const prerender = false;

const { lang } = Astro.params;
---

<MainLayout>
    Contact: {lang === undefined ? "undefined" : lang}
</MainLayout>
